{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Gravitational Force",
  "type": "object",
  "properties": {
    "type": {
      "const": "gravitational",
      "description": "Type of force; always 'gravitational'"
    },
    "applied_to": {
      "type": "string",
      "description": "The object receiving the gravitational force"
    },
    "source": {
      "type": "string",
      "description": "The object exerting the gravitational force (e.g., Earth)"
    },
    "direction": {
      "type": "string",
      "enum": ["attractive"],
      "description": "Direction of gravitational force; always attractive"
    },
    "magnitude": {
      "type": "number",
      "description": "Optional magnitude of the gravitational force in newtons (N)"
    }
  },
  "required": ["type", "applied_to", "source", "direction"]
}
