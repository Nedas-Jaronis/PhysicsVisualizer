{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Centripetal Force",
  "type": "object",
  "properties": {
    "type": { "const": "centripetal" },
    "mass": { "type": "number" },
    "velocity": { 
      "type": "object",
      "properties": {
        "x": { "type": "number"},
        "y": { "type": "number"},
        "z": { "type": "number"}
      },
      "required": ["x", "y"],
      "additionalProperties": false,
      "description": "Velocity vector of the object"
  },
    "radius": { "type": "number" },
    "applied_to": {
      "type": "string",
      "description": "ID of the object this force is applied to"
    },
    "application_point": {
      "type": "object",
      "properties": {
        "x": { "type": "number", "default": 0 },
        "y": { "type": "number", "default": 0 },
        "z": { "type": "number", "default": 0 }
      },
      "description": "Local point on the object where force is applied (optional)"
    }
  },
  "required": ["type", "mass", "velocity", "radius", "applied_to"]
}
